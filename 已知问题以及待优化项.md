# 子Agent显示类（本次排查主线）
> 说明：每条独立条目，字段固定：序号 | 现象 | 已实证根因 | 修复方案 | 状态

## 主线问题列表
1.
现象：慢Bash渲染成幻影子Agent，出现紫色卡片、文字「继续等待评审agent」、面板空行
根因：CLI对耗时≥2s前台Bash下发 task_started，task_type:"local_bash"；fork分支 normalizeClaudeTaskEvent 不校验task_type，无条件转发事件；客户端按toolId匹配，误渲染Agent容器
修复：服务端 claude-sessions.provider.ts:330 增加过滤，task_type === 'local_bash' 直接返回 null；可叠加客户端 toolName 白名单作为第二道防线
状态：方案已定，待开发

2.
现象：面板显示 "No transcript for this agent yet."，永久无法恢复
根因：useSessionSubagents.ts:104 空查询结果永久缓存；requestedAgentIds 去重，不再重试；会话卡片有内容是走实时WS流，不走HTTP查询链路
修复：缓存规则修改，已缓存为空的Agent，再次展开面板时允许重新拉取；已有有效数据的缓存保持不重复拉取
状态：方案已定，待开发

3.
现象：孤儿Agent页面刷新后从面板消失（案例：wechatgo 4个被拒df-requirement）
根因：任务中止路径下CLI用纯字符串 "User rejected" 覆盖 tool_result；父转写丢失 toolUseResult.agentId 关联；列表接口仅扫描父转写，磁盘 subagents/agent-*.jsonl + meta.json 成为孤儿数据
修复：服务端 scanClaudeSubagentMetas 扫描meta.json，读取toolUseId/agentType；对存在Agent tool_use行但缺失agentId的记录补齐绑定；孤儿Agent状态拟标记为completed（2个小决策待拍板）
状态：方案已定，2项细节决策待拍板

4.
现象：点击停止运行后子Agent永久卡在running状态（案例：wechatgo会话general-purpose长期running）
根因：isAwaitingAsyncAgent 缺少静默超时判定；被abort终止的Agent每次读取历史都会判定为running
修复：stash@{0}已有实现，SUBAGENT_LIVENESS_WINDOW_MS = 15min静默超时判定；远程环境缺失该逻辑，基于远程最新代码回填
状态：已有现成实现，待回填代码

5.
现象：任务运行期间，面板读取不到运行中子Agent的转写内容
根因：useSessionSubagents 仅单触发参数，任务运行过程不会重读子Agent列表
修复：stash已有实现，增加 isRunActive 判断 + 15s定时重读；SubagentsPanel面板代码需要适配远程重构后的结构
状态：已有现成实现，待回填并适配面板

## 关联附属问题（主线之外）
### 附属1：会话列表排序异常
现象：仅点击会话、未发送消息，会话最新触发时间被更新，引发列表重新排序

### 附属2：msg.subagent来源 & Bash是否生成子Agent
upstream原生：磁盘子Agent文件路径 `~/.claude/projects/<slug>/<session-id>/subagents/agent-<id>.jsonl`，依赖行内agentId字段；Bash不会产生该文件，Boolean(msg.subagent)恒为false，不会进入子Agent渲染分支
fork分支（39dae27之后）：新增实时SDK流通道；system/task_started事件 → normalizeClaudeTaskEvent() → subagent_update，依靠toolId写入msg.subagent；local_bash同样触发task_started，函数不校验task_type，因此误进入子Agent分支

### 附属3：/compact 命令UI与Token展示问题
执行/compact后页面显示逻辑变更；压缩后的token数值不会实时刷新，必须再发送一条命令才更新token展示

### 附属4：并发下发命令，后台子Agent回调失败报错
现象：子Agent未执行完成，直接发送新会话命令；子Agent结束后回调失败，报错文本：
No completion record was found for background agent "Resuming agent a493b3d" after it was re-dispatched via SendMessage in the previous session. It may have been stopped (via the UI, an SDK interrupt, or agent teardown — these leave no transcript marker), or it may have been running when the previous Claude Code process exited. Check its worktree/output for partial work before assuming the task landed.
根因：上一个后台Agent未结束，通过SendMessage派发新会话；Agent终止（UI停止、SDK中断、进程退出）不会留下转写标记，找不到对应的completion记录


### 附属 5：体验优化：新建 Project 目录选择无搜索能力

现象：添加新 project 的时候，目录选择器无法搜索；代码仓库目录体量较大时，查找目标文件夹操作繁琐，使用体验差
根因：新建 Project 目录选择组件未实现目录过滤搜索功能
修复：为 project 目录选择弹窗增加文件名 / 路径搜索过滤输入框，支持大目录快速定位文件夹
状态：体验优化，待评估

### 撤销模型实时读取的功能
昨天做的一个变更，需要撤销掉，直接读配置文件即可，这个功能读的不准

### 命令列表显示不全
/命令显示不全，很多原生命令没有显示，跟cli和插件端不一致，之前好像修过，但是没修复好，比如/compact、/context等命令